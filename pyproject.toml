[project]
name = "loo-parser"
version = "0.1.0"
description = "Parse poop-related whatsapp messages to record loo usage over time by user"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []
